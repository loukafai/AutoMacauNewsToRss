name: 每日自動更新澳門日報 RSS

on:
  schedule:
    # 澳門時間 07:00 (UTC+8) = UTC 23:00 (前一天)
    # 格式：分 時 日 月 週
    - cron: '0 21 * * *'
  workflow_dispatch: # 允許在 GitHub 網頁上手動點擊 "Run workflow" 執行

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write # 允許 Action 寫入與提交程式碼到 repo

    steps:
    - name: 📥 簽出程式碼 (Checkout repository)
      uses: actions/checkout@v4

    - name: 🐍 設定 Python 環境
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: 📦 安裝依賴套件
      run: |
        if [ -f requirements.txt ]; then
          pip install -r requirements.txt
        else
          pip install requests beautifulsoup4
        fi

    - name: 🚀 執行爬蟲腳本生成檔案
      run: python main.py

    - name: 💾 提交並推送到 GitHub (Commit and Push)
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "📝 自動更新今日新聞 (RSS & HTML)"
        # 同時監控並提交這兩個檔案的變動
        file_pattern: 'rss.xml index.html'
